# RX Tracker NEXT 4.0.0-next.54

## Fixed

- Prevented Project Control option 25 default test-copy naming from stacking restore suffixes when an existing restored name already contains `_restore_*`, `_test`, or `_copy` suffixes.
- Added normalization coverage in the restore helper self-test so this behavior remains protected.
- Kept existing uniqueness safety checks: when the computed target still matches the active database, operator input remains required.

## Validation

- Updated `CHANGELOG.md`, `package.json`, and `package-lock.json` to `4.0.0-next.54`.
- Ready for full staging/production release packaging workflow.

## Safety

- No schema migration or business-data rewrite in this patch.
