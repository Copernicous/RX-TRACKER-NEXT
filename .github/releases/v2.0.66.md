# Patient RX System v2.0.66

## Patient Notice-Group Carousel

This release promotes the tested staging Patients alert improvement into production.

## What Changed

- Adds a rotating notice-group carousel to the Patient Management alert banner.
- Shows `Notice N of X` so users can move through different patient task groups without leaving the page.
- Adds a `Next` button that loops through all active notice groups.
- Keeps the banner high-level: it rotates task categories, not individual patient rows.
- Keeps `Show Patients` contextual so it opens the filter for the currently visible notice group.

## Notice Groups Included

The banner automatically includes a notice only when that group has records:

- Expired 90-day window with incomplete RX workflow.
- Eligible now / past the 90-day service window.
- Service window expiring in 7 days or less.
- No service date.
- Active patients with no RX records.
- Missing required default information.

## Database Impact

- No schema changes.
- No destructive data changes.
- Existing patients, RX records, service-date cycles, workflow history, reports data, and audit records are preserved.
- The notice groups use existing dashboard, patient, RX, and missing-info filters.

## Validation Completed

- Merged tested staging changes into `develop`, then into `main`.
- JavaScript syntax checks passed for the modified Patients script and related controllers.
- EJS compile checks passed for Patients, Dashboard, RX Records, and Reports.
- Production migration check completed with schema already up to date.
- Local production/main smoke test passed on `localhost:3000`.
- Patients page rendered successfully.
- Patient pagination returned 10-row pages.
- Notice group API counts returned successfully.
- A fresh production build and update zip are generated for version `2.0.66`.

## Production Verification Checklist

After installing the `v2.0.66` update:

1. Confirm the sidebar/version badge shows `v2.0.66`.
2. Open Patient Management.
3. Confirm the alert banner shows `Notice 1 of X` when multiple task groups exist.
4. Click `Next` and confirm it cycles to the next notice group.
5. Continue clicking `Next` and confirm it loops back to the first notice.
6. Click `Show Patients` and confirm it applies the filter for the currently displayed notice.
7. Confirm Patients still defaults to 10 rows and normal filters still work.
