# Patient RX System v3.0.9

Fixes the Dashboard refresh race that replaced the Call Queue cutoff date with generic active-patient text.

The Call Pre-Eligibility card now retains text such as:

`Call Queue: Service Date on or before 04/28/2026`

No database migration is required.
