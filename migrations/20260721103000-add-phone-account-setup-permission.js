'use strict';

// Reserved migration number. The design was simplified to per-user authorization
// before release, so no role-wide permission is created.
module.exports = {
  async up() {},
  async down() {}
};
